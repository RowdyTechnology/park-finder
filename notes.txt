docker build -t parks-app . && docker run -d \
  --network web-apps \
  -p 8086:8086 \
  -v /mnt/user/appdata/parks:/mnt/user/appdata/parks \
  --name parks-app --rm parks-app

cloudflared tunnel

docker run -d --network web-apps \
-v /mnt/user/appdata/parks-cloudflared:/etc/cloudflared \
--name parks-tunnel-cloudflared crazymax/cloudflared \
tunnel --no-autoupdate --config /etc/cloudflared/config.yml run parks-tunnel

docker build -t parks-app . && docker run -d \
  --network web-apps \
  -p 8086:8086 \
  -v /mnt/user/appdata/parks:/mnt/user/appdata/parks \
  --name parks-app --rm parks-app
